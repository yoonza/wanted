- 최적화 
: 메모리를 덜 사용하는 것이 좋음. 
: 작업을 줄이고, 렌더링을 줄이는 것이 좋음. 

** UseCallback, useMemo, React.useMemo

- UseCallback : 함수를 캐싱함. ( useEffect ())

useMemo 
const myMemoValu = React.useMemo(
    () => 함수(변수들),
    [a,b,c, ...]
);

- 메모 컴포넌트 만들기 
- 부모 컴포넌트에서 메모 컴포넌트를 불러오고 
- 부모 컴포넌트로부터 메모 컴포넌트가 숫자를 세개 받아오도록 하자 
- 메모 컴포넌트에서는 숫자 세개 중 두개를 더해주는 함수를 만들자
